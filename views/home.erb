 <main>
    <h2>Principais Assuntos</h2>
        <div class="flex-container">
            <% @data.parse_json.each do |key, items| %>
            <div class="flex-card">
                    <h3><%= key %></h3>
                    <ul>
                    <% items.each do |item| %>
                        <li><%= item %></li>
                    <% end %>                
                    </ul>
            </div>
            <% end %>
        </div>
    </div>
</main>
